<project default="build">
    <property name="build.dir" value="dist" />
    <property name="conf.dir" value="conf" />
    <property name="project.name" value="CouchDBMonitor"/>
    <property name="lib.dir" value="lib"/>

    <path id="myClassPath">
        <fileset dir="${lib.dir}" includes="**/*.jar"/>
        <fileset dir="${conf.dir}" includes="*.*"/>

    </path>

    <target name="build">
        <mkdir dir="${build.dir}/classes"/>
        <javac destdir="${build.dir}/classes" source="1.5" target="1.5" srcdir="src/" classpathref="myClassPath" debug="on"/>
        <mkdir dir="${build.dir}/CouchDBMonitor"/>
        <mkdir dir="${build.dir}/CouchDBMonitor/lib"/>
        <mkdir dir="${build.dir}/CouchDBMonitor/conf"/>

        <jar destfile="${build.dir}/CouchDBMonitor/${project.name}.jar">
            <fileset dir="${build.dir}/classes"/>
            <manifest>
                <attribute name="Class-Path" value="${build.dir}/${project.name}/${project.name}.jar"/>
            </manifest>
        </jar>
        <copy todir="${build.dir}/CouchDBMonitor/lib/">
            <fileset dir="${lib.dir}" includes="*.jar"/>
        </copy>
        <copy todir="${build.dir}/CouchDBMonitor/">
            <fileset dir="${conf.dir}" includes="monitor.xml"/>
        </copy>
        <copy todir="${build.dir}/CouchDBMonitor/${conf.dir}">
            <fileset dir="${conf.dir}" includes="*.*" excludes="monitor.xml"/>
        </copy>
        <delete dir="${build.dir}/classes"/>
    </target>
    <target name="package" depends="build">
        <zip destfile="${build.dir}/CouchDBMonitor.zip">
            <zipfileset dir="${build.dir}/CouchDBMonitor" filemode="755" prefix="CouchDBMonitor/"/>
        </zip>
        <delete dir="${build.dir}/CouchDBMonitor"/>
    </target>
    <target name="clean">
        <delete dir="${build.dir}"/>
    </target>
</project>
